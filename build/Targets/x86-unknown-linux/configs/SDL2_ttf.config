#!/bin/bash

./configure --with-sdl-prefix=$SDL_PREFIX \
            --with-sdl-exec-prefix=$SDL_PREFIX \
            --with-freetype-prefix=$FREETYPE_PREFIX \
            --with-freetype-exec-prefix=$FREETYPE_PREFIX
